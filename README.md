# RevealLine archive29: v0.61.23 plus proposed v0.61.24

Retain the exact original v0.61.23 and v0.61.24 distributions. The accepted v623 cohort and all its metadata remain byte-identical. New v624 is release 391850171, source `bb8cd8ea8af0cbec6af7e633c05d1bec78815938`, tree `9f0f268509970670bfc366f258a0eb969ccf3164`, annotated tag `d409071affdf4bb9dbbc2c3f24f4675dbcccfc85`. No game source, artwork, original ZIP or source qualification is rebuilt or amended. The original `input-authority.json` remains unchanged; `append-v06124-authority.json` records this append.

The expected artifact has **1,401 files / 626,907,986 bytes**, leaving **173,092,014 bytes** under the unchanged 800,000,000-byte cap. From the accepted 702-row inventory, 699 v623 release rows and two support rows remain exact: **701 preserved paths**. Only root `index.html` changes to add the v624 link; `.nojekyll` and `releases/index.html` stay byte-identical. **699 v624 rows / 313,457,732 bytes** are appended. Counts are derived from every original manifest and qualification row, including hidden build markers.

Accepted archive main `09c84ec4c468d7c78cf73e2f880420ecbf314c43` (tree `b28634fa2778ae74fb24a0e8ae4f7f1d33df83ac`) passed 702/702 public bodies without retry/failure and scoped keyboard gameplay/Pause/Resume plus explorer navigation. Its source checkout `5aea7867a1dd0e8b2640ce399311bc6136337854` has the same tree. v624's separate public-main acceptance covered the scoped Solo Pause layout correction and 3,280 deployed bodies; neither previous acceptance qualifies this new archive deployment.

The workflow explicitly fetches both immutable tags, v0.61.23 and v0.61.24, before running the unchanged extractor pinned at `a13ab970222498d7c5fa7f62f9fc04fe436979d5`. Prepare/verify helpers, main-only gating, noncancelling concurrency, no stored checkout credentials, 3 GiB hosted free-space guard, 800 MB / 20,000-row limits, original tag/source checks, ZIP CRC/hash checks and complete hidden-file byte reread stay unchanged.

This append is prepared locally only. A new archive commit/run/deployment, complete public HTTP audit and native retention acceptance remain pending. Root must review exact changes, adopt from fresh accepted main, merge once, observe the automatic push workflow, retain the small original receipts, reconcile all expected rows with unchanged before/after authorities, and test scoped actual entry/play/Pause/Resume/explorer return in both retained editions. Keep physical hardware, audio/offline, zoom and complete parent phases open; shared origin does not prove independent save ownership.

Main must admit the new archive evidence only after those checks. Preserve all prior archive-29 evidence and every other admission. The main selector remains v0.61.24 until v0.62.0 independently qualifies and is published; all-history explorer routing and 950 MB main / 800 MB archive caps remain unchanged.
